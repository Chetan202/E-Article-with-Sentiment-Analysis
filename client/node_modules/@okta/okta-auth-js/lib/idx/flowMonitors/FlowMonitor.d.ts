import { Remediator, RemediationValues, SkipValues } from '../remediators';
import { IdxRemediation } from '../types/idx-js';
export declare class FlowMonitor {
    previousRemediator: Remediator;
    shouldBreak(remediator: Remediator): boolean;
    isRemediatorCandidate(remediator: Remediator, remediations?: IdxRemediation[], values?: RemediationValues & SkipValues): boolean;
}
