import { Remediator, RemediationValues } from './remediators';
import { RunOptions } from './run';
import { NextStep, IdxMessage } from './types';
import { IdxResponse, IdxRemediation } from './types/idx-js';
interface RemediationResponse {
    idxResponse?: IdxResponse;
    nextStep?: NextStep;
    messages?: IdxMessage[];
    terminal?: boolean;
    canceled?: boolean;
}
export declare function getRemediator(idxRemediations: IdxRemediation[], values: RemediationValues, options: RunOptions): Remediator;
export declare function remediate(idxResponse: IdxResponse, values: RemediationValues, options: RunOptions): Promise<RemediationResponse>;
export {};
